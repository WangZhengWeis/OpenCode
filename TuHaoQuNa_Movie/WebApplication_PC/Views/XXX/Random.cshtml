@model List<Model.RARGB.Torrent>
<script src="/static/js/ReferreerKiller.js"></script>
<div style="padding:5px;padding-bottom:10px;border-bottom:2px #ccc dotted">
    <h4 class="yjbjs">随机推荐</h4>
    @for (int i = 0; i < 10; i++)
    {
        <a target="_blank" href="/XXX/detailte/@Model[i].ID">
            <div style="position:relative;float:left">
                @*<img class="myimg" style="width:138px;height:190px" title="@Model[i].Title" src="@Model[i].Poster">&nbsp;*@
                <script>document.write(ReferrerKiller.imageHtml("@(Model[i].Poster)"));</script>
                <div style="background: rgba(0, 0, 0, 0.6);color: #FFF;display: block;left: 0;right: 0;position: absolute;bottom: 0;font-size: 12px;text-align: center;margin:0px 4px 0px 0px;"><span>@Model[i].Title</span></div>
            </div>
        </a>
    }
    <table>
        @for (int i = 10; i < Model.Count; i++)
        {
            var m = Model[i];
            <tr style="height:30px;line-height:30px;border-top:1px #ccc dotted">
                <td style="width:260px"><a target="_blank" style="font-size:12px" href="/movie/detailte/@m.ID">@m.Title</a></td>
                @*<td><span style="color:red;font-size:12px">@String.Format("{0:N1}", m.Score)</span></td>*@
            </tr>
        }
    </table>
</div>
<script>
    //var imgs = document.getElementsByClassName("myimg");
    //for (var i = 0; i < imgs.length; i++) {
    //    var src = imgs[0].getAttribute("src");
    //    imgs = ReferrerKiller.imageHtml(src);
    //}
    //$(".myimg").each(function (i) {
    //    var src = $(tihs).attr("src");
    //    $(this).html(ReferrerKiller.imageHtml(src));
    //});
</script>